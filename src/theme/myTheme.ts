import { createTheme, ThemeOptions } from '@mui/material';

export const myTheme: ThemeOptions = createTheme({
    palette: {
        primary: {
            main: '#D61111'  
        },
        secondary: {
            main: '#154FB9'
        },
        background: {
            paper: '#237bda'
        }
    }
});